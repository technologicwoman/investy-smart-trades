const Strategy = () => (
  <div className="flex min-h-screen items-center justify-center bg-background">
    <p className="text-muted-foreground">Strategy — coming soon</p>
  </div>
);
export default Strategy;
